<div class="jumbotron">

    <h1 class="display-4">Bem-vindo ao Blog do Node</h1>
    <p class="lead">Este é um blog simples, desenvolvido com Node.js + Express + MongoDB.</p>
    <hr class="my-4">
    <p>Aqui você vai poder ler sobre as novidades do mundo da computação.</p>
    {{#if user}}
    <h3>
        Olá, sejá bem vindo(a), {{user.nome}}
    </h3>
    {{else}}
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="/usuarios/login" role="button">Acessar</a>
    <p>Login administrador: admin@admin.com</p>
    <p>Senha: 123123</p>
    </p>
    {{/if}}

</div>

<hr>

<h2>Postagens recentes:</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descrição}}</p>
        <a href="/postagem/{{slug}}">
            <button class="btn btn-success">Leia meia</button>
        </a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>

{{else}}
<h5>Nenhuma postagem</h5>
{{/each}}